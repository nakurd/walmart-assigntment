package com.walmart.service

import spock.lang.Specification

class TicketServiceWalmartImplSpec extends Specification{
	 
	public void "maximum of two numbers"() {
		expect:
			Math.max(a, b) == c
		where:
			a | b  || c
			3 | 5  || 5
			7 | 0  || 7
			0 | 0  || 0
		   -1 | -2 || -1
	}
	
}
